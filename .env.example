# API Passanota
PASSANOTA_API_URL=http://localhost:8000
# Desabilita token IAM em dev local (API sem Cloud Run auth)
PASSANOTA_API_USE_IAM=false

# Supabase Auth (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# Em Cloud Run o deploy define SUPABASE_URL / SUPABASE_PUBLISHABLE_KEY em runtime (middleware/server).

# URL do frontend (redirect magic link — deve coincidir com Supabase Auth URLs)
NEXT_PUBLIC_APP_URL=http://localhost:3000
